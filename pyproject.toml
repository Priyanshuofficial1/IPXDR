[project]
name = "diodesentinel"
version = "0.1.0"
description = "Passive, self-adapting AI threat detection for unidirectional IP traffic"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
